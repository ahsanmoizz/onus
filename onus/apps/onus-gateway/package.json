{
  "name": "onus-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Managed Onus semantic-review gateway for server-side provider credentials.",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "pg": "^8.12.0"
  },
  "engines": {
    "node": ">=20"
  }
}
